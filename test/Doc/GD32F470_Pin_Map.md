# GD32F470 梁山派开发板引脚占用说明文档

本文档详细列出了当前 Keil 工程（包含 CS1237 ADC、DAC 调试、HLW8112 电能计量系统以及 YT8512C 以太网系统）中已被占用的 MCU 引脚及其功能用途，以防后续外设开发时发生引脚冲突。

---

## 引脚分配总表

| 序号 | 端口引脚 | 外设模块 | 配置模式 | I/O方向 | 默认电平/状态 | 功能描述 |
| :---: | :---: | :---: | :---: | :---: | :---: | :--- |
| 1 | **PA9** | **USART0** | 复用模式 (AF7) | 输出 (Push-Pull) | - | 调试串口发送端 (TX)，用于串口输出调试数据 |
| 2 | **PA10** | **USART0** | 复用模式 (AF7) | 输入 (Pull-Up) | - | 调试串口接收端 (RX)，接收控制指令 |
| 3 | **PA4** | **DAC0_OUT0** | 模拟模式 (Analog) | 输出 | - | 模拟电压输出通道 0 (DAC0) |
| 4 | **PA5** | **DAC0_OUT1** | 模拟模式 (Analog) | 输出 | - | 模拟电压输出通道 1 (DAC1) |
| 5 | **PC3** | **CS1237 ADC** | 普通输出 (GPIO Output) | 输出 (Push-Pull) | LOW | ADC 串行时钟线 (SCLK)，输出时钟脉冲 |
| 6 | **PC2** | **CS1237 ADC** | 双向输入输出 (GPIO) | 输入(上拉) / 输出 | - | ADC 串行数据线 (DOUT/DRDY)，收发数据与检测就绪 |
| 7 | **PD7** | **LED2** | 普通输出 (GPIO Output) | 输出 (Push-Pull) | - | 板载 LED2 控制脚，用于指示运行状态 |
| 8 | **PB11** | **HLW8112** | 普通输出 (GPIO Output) | 输出 (Push-Pull) | HIGH | 芯片使能/复位引脚 (EN/RSTN)，高电平使能，低电平复位 |
| 9 | **PB12** | **HLW8112** | 普通输出 (GPIO Output) | 输出 (Push-Pull) | HIGH | 模拟 SPI 片选线 (SCSN)，拉低以启动通信 |
| 10 | **PB13** | **HLW8112** | 普通输出 (GPIO Output) | 输出 (Push-Pull) | LOW | 模拟 SPI 时钟线 (SCLK)，空闲时保持低电平 |
| 11 | **PB14** | **HLW8112** | 普通输入 (GPIO Input) | 输入 (Pull-Up) | - | 模拟 SPI 数据输出线 (SDO/MISO)，接收计量数据 |
| 12 | **PB15** | **HLW8112** | 普通输出 (GPIO Output) | 输出 (Push-Pull) | LOW | 模拟 SPI 数据输入线 (SDI/MOSI)，发送控制指令 |
| 13 | **PA1** | **YT8512C** | 复用模式 (AF11) | 输入 | - | RMII 50MHz 参考时钟输入 (REF_CLK) |
| 14 | **PA2** | **YT8512C** | 复用模式 (AF11) | 双向输入输出 | 内部上拉 | SMI 管理数据线 (MDIO) |
| 15 | **PA7** | **YT8512C** | 复用模式 (AF11) | 输入 | - | RMII 接收数据有效信号 (CRS_DV) |
| 16 | **PC1** | **YT8512C** | 复用模式 (AF11) | 输出 (Push-Pull) | - | SMI 管理数据时钟线 (MDC) |
| 17 | **PC4** | **YT8512C** | 复用模式 (AF11) | 输入 | - | RMII 接收数据线 0 (RXD0) |
| 18 | **PC5** | **YT8512C** | 复用模式 (AF11) | 输入 | - | RMII 接收数据线 1 (RXD1) |
| 19 | **PG11** | **YT8512C** | 复用模式 (AF11) | 输出 (Push-Pull) | - | RMII 发送数据使能信号 (TXEN)（避让PB11） |
| 20 | **PG13** | **YT8512C** | 复用模式 (AF11) | 输出 (Push-Pull) | - | RMII 发送数据线 0 (TXD0)（避让PB12） |
| 21 | **PG14** | **YT8512C** | 复用模式 (AF11) | 输出 (Push-Pull) | - | RMII 发送数据线 1 (TXD1)（避让PB13） |
| 22 | **PA3** | **YT8512C** | 普通输出 (GPIO Output) | 输出 (Push-Pull) | HIGH | PHY 硬件复位引脚 (RESET) |

---

## 各外设模块引脚详情说明

### 1. 调试串口 (USART0)
* **引脚**：PA9 (TX), PA10 (RX)
* **配置源文件**：`usart.c` / `usart.h`
* **说明**：配置为 AF7 复用功能，波特率通常初始化为 `115200`，供 `printf` 重定向输出。

### 2. 数模转换 (DAC)
* **引脚**：PA4 (通道0), PA5 (通道1)
* **配置源文件**：`bsp_dac.c` / `bsp_dac.h`
* **说明**：配置为模拟（Analog）通道，当前在 `main.c` 中已被注释掉，若后续需要使用板载 DAC 输出测试电压，请直接取消 `main.c` 中 `bsp_dac_init()` 等函数的注释。

### 3. 高精度模数转换 (CS1237 ADC)
* **引脚**：PC3 (SCLK), PC2 (DOUT)
* **配置源文件**：`bsp_cs1237.c` / `bsp_cs1237.h`
* **说明**：
  * **PC3 (SCLK)**：时钟输出引脚。由于 CS1237 芯片空闲时钟需保持为低电平且时钟高电平 $>100\mu\text{s}$ 会使芯片进入休眠，因此该引脚默认为 **低电平**。
  * **PC2 (DOUT)**：这是一个双向引脚，数据读取阶段作为**带上拉输入**；配置寄存器阶段会临时切换为**推挽输出**。

### 4. 状态指示灯 (LED2)
* **引脚**：PD7
* **配置源文件**：`main.c`
* **说明**：用于开发板板载的蓝色 LED 灯闪烁控制。

### 5. 电能电力计量 (HLW8112)
* **引脚**：PB11 (EN/RSTN), PB12 (SCSN), PB13 (SCLK), PB14 (SDO/MISO), PB15 (SDI/MOSI)
* **配置源文件**：`bsp_hlw8112.c` / `bsp_hlw8112.h`
* **说明**：
  * **PB11 (EN/RSTN)**：芯片的硬件使能引脚，拉高使能芯片，拉低会使芯片复位。
  * **PB12 (SCSN)**：片选引脚，低电平使能 SPI 通信。
  * **PB13 (SCLK)**：模拟 SPI 时钟脚，空闲状态（CPOL=0）保持低电平。
  * **PB14 (SDO/MISO)**：MCU 接收数据脚，设置为带上拉输入。
  * **PB15 (SDI/MOSI)**：MCU 发送数据脚，设置为推挽输出。

### 6. 以太网 PHY (YT8512C)
* **引脚**：PA1 (REF_CLK), PA2 (MDIO), PA3 (RESET), PA7 (CRS_DV), PC1 (MDC), PC4 (RXD0), PC5 (RXD1), PG11 (TXEN), PG13 (TXD0), PG14 (TXD1)
* **配置源文件**：`bsp_yt8512c.c` / `bsp_yt8512c.h`
* **说明**：
  * **SMI (MDC/MDIO)**：用于配置 PHY 寄存器。由于开发板上外部上拉可能偏弱，`PA2 (MDIO)` 在 MCU 端配置为了开启内部上拉。
  * **RMII TX 信号**：为了避免与 HLW8112 占用的 `PB11~PB13` 引脚冲突，硬件上将 TX 接口迁移到了 `PG11`、`PG13`、`PG14`。
  * **PA3 (RESET)**：普通 GPIO，低电平复位，拉高后芯片运行。

---

## 避坑与扩展指南

* **引脚复用注意**：
  在添加其他外设（如 I2C 或 PWM 脉宽调制）时，应避开以下已被占用的引脚：
  * **PA1, PA2, PA3, PA4, PA5, PA7, PA9, PA10**
  * **PB11, PB12, PB13, PB14, PB15**
  * **PC1, PC2, PC3, PC4, PC5**
  * **PD7**
  * **PG11, PG13, PG14**
* **VREF 与模拟地 (AGND) 注意**：
  在差分测量微小电压时，应保证 AIN- 与 AIN+ 外部线路远离高频数字信号线（如 PA9, PA10 串口线和 PC3 时钟线），并确保 GND 接触良好，以防引入高频噪声干扰 ADC 采样。
